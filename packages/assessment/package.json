{
  "name": "@prepdog/assessment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  }
}