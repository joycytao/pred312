{
  "name": "@prepdog/firebase",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@prepdog/content": "workspace:*",
    "firebase": "^12.6.0",
    "firebase-admin": "^13.6.0"
  },
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json"
  }
}